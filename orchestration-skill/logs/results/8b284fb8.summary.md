VERDICT: NOT READY
Health Score: 55%
Top Strengths:
- Feature surface is comprehensive: auth, child profiles, avatar generation, story engine, credits, PDF export, and cron jobs are all implemented across web/mobile + tRPC (`packages/api/src/routers/*`).
- Strong guardrails: rate limits, audit logging, ownership checks, and COPPA consent enforcement are present in the API layer (e.g., `packages/api/src/routers/auth.ts`, `avatar.ts`).
- Payments/webhooks use proper signature verification and ledger updates (Stripe route in `apps/web/app/api/webhooks/stripe/route.ts`).
Top Concerns/Blockers:
- AI clients throw at import time when `GOOGLE_CLOUD_PROJECT` is unset; env template omits this, so the server cannot start in a fresh prod env (`packages/ai/src/imagen/client.ts:489-505`, `packages/ai/src/gemini/client.ts:55-60`).
- Story/Avatar generation relies on Vertex AI + Supabase storage credentials that are not validated or documented in `.env.example`, so core flows (avatars/stories) will fail until keys/service accounts and buckets are provisioned.
- Test/CI state unknown: 54 test files exist but were not executed in this assessment; migrations were not validated, so runtime regressions or schema drift may exist.
Critical Issues Before Launch:
- Add required Google/Vertex env vars (project id + service account creds) to configuration and guard imports so services don’t crash when unset.
- Verify Supabase buckets/keys for stories, temp uploads, and PDFs are configured; run `prisma migrate status` against the target DB to confirm schema matches code.
- Run the full lint/test/build pipeline (pnpm lint/test/build) in a production-like env to catch compile/runtime failures, especially across Next 15 RC + React 19 RC.
